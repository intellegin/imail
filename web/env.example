# 🌱 Environment variables for the iMAIL web app
# Copy or rename this file to ".env" (keep it inside the web/ directory) and
# replace the placeholder values with your own.
#
# IMPORTANT: Only variables that start with the VITE_ prefix are exposed to
# the client-side code by Vite. Do NOT store any secrets here.

# Base URL for your backend API (no trailing slash)
VITE_API_BASE_URL=https://api.example.com

# Auth0 configuration
VITE_AUTH0_DOMAIN=your-tenant.auth0.com
VITE_AUTH0_CLIENT_ID=your-client-id
VITE_AUTH0_AUDIENCE=your-api-identifier

# Optional arbitrary variables
VITE_APP_ENV=development 